package x402types

// Version pins the x402 wire conformance target: the coinbase x402 "typical
// flow", `exact`-scheme handshake (spec 0005, Decision 7). It is not part of
// the generated schema because it is a fixed protocol constant, not a field.
const Version = 1
